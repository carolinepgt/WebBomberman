{% extends "layout.html.twig" %}
{% block contenu %}
    <form method="post" action="{{ path('friends.validFormDelete') }}">
        <div class="row">
            <fieldset>
                <input type="hidden"   name="_csrf_token"   value="{{ csrf_token('token') }}">
                <legend>Confirmer la suppression de cet ami : </legend>
                <input name="id"  type="hidden" value="{{donneesAmi.idUSerAjoute}}" />
                <input name="id2"  type="hidden" value="{{donneesAmi.idUserAjoutant}}" />
                <label>Nom : <h5>{{donneesAmi.loginPlayerAjoute}}</h5> </label>
                <input type="hidden" id="_method" name="_method" value="delete" />

                <input type="submit" name="supprimerAmi" value="Supprimer un ami" />
            </fieldset>
        </div>
    </form>
{% endblock %}